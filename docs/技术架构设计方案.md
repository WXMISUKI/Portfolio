# 技术架构设计方案

## 📋 文档信息

- **项目名称**: 个人作品集网站
- **文档版本**: v1.0
- **创建日期**: 2026-04-12
- **架构师**: 罗雍来

---

## 🎯 架构目标

### 业务目标
1. **求职展示**: 向HR和技术面试官展示个人技术实力和项目经验
2. **技术品牌**: 建立个人技术品牌,展示技术深度和广度
3. **用户体验**: 提供流畅、专业的浏览体验

### 技术目标
1. **高性能**: 页面加载速度 < 2秒,Lighthouse分数 > 90
2. **可维护性**: 代码结构清晰,易于维护和扩展
3. **可扩展性**: 支持功能模块的快速添加和修改
4. **响应式**: 完美适配桌面端、平板、移动端
5. **SEO友好**: 搜索引擎优化,提升可见性

---

## 🏗️ 整体架构

### 架构分层

```
┌─────────────────────────────────────────────────────┐
│                   用户界面层 (UI)                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ 首页     │  │ 项目展示 │  │ 技术栈   │  ...     │
│  └──────────┘  └──────────┘  └──────────┘          │
├─────────────────────────────────────────────────────┤
│                  组件层 (Components)                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ 布局组件 │  │ 业务组件 │  │ UI组件   │          │
│  └──────────┘  └──────────┘  └──────────┘          │
├─────────────────────────────────────────────────────┤
│                 业务逻辑层 (Composables)              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ 滚动交互 │  │ 主题切换 │  │ 数据获取 │          │
│  └──────────┘  └──────────┘  └──────────┘          │
├─────────────────────────────────────────────────────┤
│                   数据层 (Store)                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ 项目数据 │  │ 用户交互 │  │ 系统配置 │          │
│  └──────────┘  └──────────┘  └──────────┘          │
├─────────────────────────────────────────────────────┤
│                   路由层 (Router)                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ 路由配置 │  │ 导航守卫 │  │ 路由过渡 │          │
│  └──────────┘  └──────────┘  └──────────┘          │
├─────────────────────────────────────────────────────┤
│                   基础设施层 (Infrastructure)          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ HTTP请求 │  │ 工具函数 │  │ 类型定义 │          │
│  └──────────┘  └──────────┘  └──────────┘          │
└─────────────────────────────────────────────────────┘
```

### 核心模块架构

```
Portfolio/
│
├── Core (核心模块)
│   ├── Application (应用入口)
│   │   ├── App.vue
│   │   └── main.ts
│   │
│   ├── Router (路由管理)
│   │   ├── index.ts (路由配置)
│   │   └── guards.ts (路由守卫)
│   │
│   └── Store (状态管理)
│       ├── project.ts (项目数据)
│       ├── ui.ts (UI状态)
│       └── index.ts (Store导出)
│
├── Features (功能模块)
│   ├── Home (首页)
│   │   ├── views/Home.vue
│   │   ├── components/HeroSection.vue
│   │   └── components/QuickLinks.vue
│   │
│   ├── About (关于我)
│   │   ├── views/About.vue
│   │   ├── components/ProfileCard.vue
│   │   ├── components/SkillRadar.vue
│   │   └── components/Timeline.vue
│   │
│   ├── Projects (项目展示)
│   │   ├── views/Projects.vue
│   │   ├── components/ProjectCard.vue
│   │   ├── components/ProjectFilter.vue
│   │   ├── components/ProjectSearch.vue
│   │   └── components/ProjectDetailModal.vue
│   │
│   ├── Skills (技术栈)
│   │   ├── views/Skills.vue
│   │   ├── components/SkillCategory.vue
│   │   ├── components/SkillTag.vue
│   │   └── components/SkillProgress.vue
│   │
│   └── Contact (联系我)
│       ├── views/Contact.vue
│       ├── components/ContactForm.vue
│       ├── components/ContactCard.vue
│       └── components/SocialLinks.vue
│
├── Shared (共享模块)
│   ├── Components (公共组件)
│   │   ├── Layout/
│   │   │   ├── TheHeader.vue
│   │   │   ├── TheFooter.vue
│   │   │   └── BackToTop.vue
│   │   ├── Common/
│   │   │   ├── SectionTitle.vue
│   │   │   ├── LoadingSpinner.vue
│   │   │   └── ErrorBoundary.vue
│   │   └── UI/
│   │       ├── Button.vue
│   │       ├── Link.vue
│   │       ├── Badge.vue
│   │       └── Modal.vue
│   │
│   ├── Composables (组合式函数)
│   │   ├── useScrollReveal.ts
│   │   ├── useTheme.ts
│   │   ├── useSmoothScroll.ts
│   │   └── useIntersectionObserver.ts
│   │
│   ├── Utils (工具函数)
│   │   ├── format.ts
│   │   ├── validate.ts
│   │   ├── storage.ts
│   │   └── performance.ts
│   │
│   └── Types (类型定义)
│       ├── project.ts
│       ├── skill.ts
│       └── common.ts
│
└── Assets (资源)
    ├── Styles (样式)
    │   ├── main.css
    │   ├── variables.css
    │   └── animations.css
    │
    ├── Data (静态数据)
    │   ├── projects.ts
    │   ├── skills.ts
    │   └── profile.ts
    │
    └── Images (图片)
        ├── projects/
        ├── avatar/
        └── icons/
```

---

## 🛠️ 技术选型

### 核心框架

#### Vue 3.5+
**选择理由**:
- Composition API提供更好的代码组织和复用性
- 优秀的性能和体积
- 完善的TypeScript支持
- 活跃的社区和生态

**关键特性**:
- Composition API: 更好的逻辑复用
- Teleport: 更灵活的组件挂载
- Suspense: 异步组件加载
- 性能优化: 更小的bundle和更快的渲染

#### TypeScript 5.0+
**选择理由**:
- 类型安全,减少运行时错误
- 更好的IDE支持和智能提示
- 提升代码可维护性
- 便于团队协作

**使用策略**:
- 100% TypeScript覆盖
- 严格的类型检查
- 接口和类型定义分离
- 泛型和工具类型充分利用

### 构建工具

#### Vite 6.0+
**选择理由**:
- 极快的开发服务器启动速度
- 基于ESM的原生模块支持
- 优秀的HMR(热模块替换)
- 丰富的插件生态

**关键配置**:
```typescript
export default defineConfig({
  plugins: [vue(), UnoCSS()],
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          'vue-vendor': ['vue', 'vue-router', 'pinia'],
          'utils': ['lodash-es', 'dayjs'],
        },
      },
    },
    minify: 'terser',
  },
});
```

### 状态管理

#### Pinia 3.0+
**选择理由**:
- Vue 3官方推荐的状态管理库
- 轻量级,API简洁
- 完整的TypeScript支持
- 模块化设计,易于组织

**Store结构**:
```typescript
// store/modules/project.ts
export const useProjectStore = defineStore('project', {
  state: () => ({
    projects: [] as Project[],
    currentProject: null as Project | null,
    isLoading: false,
  }),

  getters: {
    activeProjects: (state) => state.projects.filter(p => p.active),
    projectsByCategory: (state) => {
      return state.projects.reduce((acc, project) => {
        if (!acc[project.category]) {
          acc[project.category] = [];
        }
        acc[project.category].push(project);
        return acc;
      }, {} as Record<string, Project[]>);
    },
  },

  actions: {
    async loadProjects() {
      this.isLoading = true;
      try {
        const data = await import('@/assets/data/projects');
        this.projects = data.default;
      } finally {
        this.isLoading = false;
      }
    },
  },
});
```

### 路由管理

#### Vue Router 4.5+
**选择理由**:
- Vue 3官方路由库
- 完整的TypeScript支持
- 灵活的导航守卫
- 优秀的性能

**路由配置**:
```typescript
const routes: RouteRecordRaw[] = [
  {
    path: '/',
    name: 'Home',
    component: () => import('@/features/Home/views/Home.vue'),
    meta: {
      title: '首页',
      description: '罗雍来 - 前端工程 & AI全栈开发者',
    },
  },
  {
    path: '/about',
    name: 'About',
    component: () => import('@/features/About/views/About.vue'),
    meta: {
      title: '关于我',
      description: '了解更多关于我的信息',
    },
  },
  {
    path: '/projects',
    name: 'Projects',
    component: () => import('@/features/Projects/views/Projects.vue'),
    meta: {
      title: '项目经验',
      description: '我的项目经验和成果展示',
    },
  },
  {
    path: '/skills',
    name: 'Skills',
    component: () => import('@/features/Skills/views/Skills.vue'),
    meta: {
      title: '技术栈',
      description: '我的技术栈和技能展示',
    },
  },
  {
    path: '/contact',
    name: 'Contact',
    component: () => import('@/features/Contact/views/Contact.vue'),
    meta: {
      title: '联系我',
      description: '联系方式和合作机会',
    },
  },
];
```

### 样式方案

#### UnoCSS 0.60+
**选择理由**:
- 原子化CSS,极致的性能
- 即时生成,无需预扫描
- 完整的TypeScript智能提示
- 丰富的预设和快捷方式

**配置示例**:
```typescript
export default defineConfig({
  presets: [
    presetUno(),
    presetIcons({
      scale: 1.2,
      warn: true,
    }),
  ],
  shortcuts: {
    'container-custom': 'max-w-7xl mx-auto px-4 sm:px-6 lg:px-8',
    'section-padding': 'py-20 px-4',
    'card-base': 'bg-[#0F172A]/80 backdrop-blur-sm rounded-xl p-6 border border-[#1E293B]',
    'card-hover': 'hover:border-[#06B6D4] hover:shadow-lg hover:shadow-[#06B6D4]/10 transition-all duration-300',
    'btn-primary': 'bg-[#06B6D4] text-[#020617] px-6 py-2 rounded-lg font-medium hover:bg-[#0891B2] transition-colors duration-300',
  },
  theme: {
    colors: {
      primary: '#0F172A',
      accent: '#06B6D4',
      background: '#020617',
      text: '#E2E8F0',
    },
  },
});
```

### 工具库

#### Lodash-es
- 使用ES Module版本,支持tree-shaking
- 按需引入,减少bundle体积

#### Day.js
- 轻量级日期处理库
- API设计优雅,易于使用

#### VueUse
- Vue 3组合式函数集合
- 提供丰富的工具函数

---

## 📊 数据流设计

### 数据架构

```
┌─────────────────────────────────────────────────────┐
│                    静态数据层                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ 项目数据 │  │ 技术栈   │  │ 个人信息 │          │
│  └──────────┘  └──────────┘  └──────────┘          │
└─────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────┐
│                    Pinia Store                        │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ Project  │  │ UI Store │  │ Config   │          │
│  │  Store   │  │          │  │  Store   │          │
│  └──────────┘  └──────────┘  └──────────┘          │
└─────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────┐
│                    组件层                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ 视图组件 │  │ 业务组件 │  │ UI组件   │          │
│  └──────────┘  └──────────┘  └──────────┘          │
└─────────────────────────────────────────────────────┘
```

### 数据类型定义

#### 项目数据类型
```typescript
// types/project.ts
export interface Project {
  id: string;
  title: string;
  category: 'government' | 'ai' | 'personal';
  type: string;
  role: string;
  techStack: string[];
  highlights: string[];
  screenshots: string[];
  repo?: string;
  demo?: string;
  metrics?: {
    performance?: string;
    codeLines?: string;
    duration?: string;
  };
  createdAt: string;
  updatedAt: string;
}

export interface ProjectFilter {
  category?: string;
  techStack?: string[];
  search?: string;
}
```

#### 技术栈数据类型
```typescript
// types/skill.ts
export interface Skill {
  id: string;
  name: string;
  category: string;
  proficiency: number; // 1-5
  icon?: string;
  projects?: string[]; // 项目ID列表
  learnedAt: string;
}

export interface SkillCategory {
  id: string;
  name: string;
  icon: string;
  skills: Skill[];
}
```

#### 个人信息类型
```typescript
// types/profile.ts
export interface Profile {
  name: string;
  title: string;
  subtitle: string;
  description: string;
  avatar: string;
  tags: string[];
  contact: {
    phone: string;
    email: string;
    github: string;
    linkedin?: string;
  };
  jobExpectations: {
    positions: string[];
    cities: string[];
    salary: string;
  };
}
```

### 状态管理

#### Project Store
```typescript
export const useProjectStore = defineStore('project', {
  state: (): ProjectState => ({
    projects: [],
    currentProject: null,
    filter: {
      category: undefined,
      techStack: [],
      search: '',
    },
    isLoading: false,
  }),

  getters: {
    filteredProjects: (state): Project[] => {
      let result = state.projects;

      // 按类别筛选
      if (state.filter.category) {
        result = result.filter(p => p.category === state.filter.category);
      }

      // 按技术栈筛选
      if (state.filter.techStack.length > 0) {
        result = result.filter(p =>
          state.filter.techStack!.some(tech =>
            p.techStack.includes(tech)
          )
        );
      }

      // 按搜索关键词筛选
      if (state.filter.search) {
        const search = state.filter.search.toLowerCase();
        result = result.filter(p =>
          p.title.toLowerCase().includes(search) ||
          p.type.toLowerCase().includes(search)
        );
      }

      return result;
    },

    projectById: (state) => (id: string): Project | undefined => {
      return state.projects.find(p => p.id === id);
    },

    allTechStacks: (state): string[] => {
      const techs = new Set<string>();
      state.projects.forEach(project => {
        project.techStack.forEach(tech => techs.add(tech));
      });
      return Array.from(techs).sort();
    },
  },

  actions: {
    async loadProjects() {
      this.isLoading = true;
      try {
        const data = await import('@/assets/data/projects');
        this.projects = data.default;
      } finally {
        this.isLoading = false;
      }
    },

    setFilter(filter: Partial<ProjectFilter>) {
      this.filter = { ...this.filter, ...filter };
    },

    resetFilter() {
      this.filter = {
        category: undefined,
        techStack: [],
        search: '',
      };
    },

    setCurrentProject(project: Project | null) {
      this.currentProject = project;
    },
  },
});
```

#### UI Store
```typescript
export const useUIStore = defineStore('ui', {
  state: (): UIState => ({
    theme: 'dark',
    isMobileMenuOpen: false,
    isProjectDetailOpen: false,
    scrollPosition: 0,
  }),

  actions: {
    toggleMobileMenu() {
      this.isMobileMenuOpen = !this.isMobileMenuOpen;
    },

    closeMobileMenu() {
      this.isMobileMenuOpen = false;
    },

    toggleProjectDetail() {
      this.isProjectDetailOpen = !this.isProjectDetailOpen;
    },

    updateScrollPosition(position: number) {
      this.scrollPosition = position;
    },
  },
});
```

---

## ⚡ 性能优化策略

### 加载优化

#### 1. 路由懒加载
```typescript
const routes: RouteRecordRaw[] = [
  {
    path: '/',
    component: () => import('@/features/Home/views/Home.vue'),
  },
  // 其他路由...
];
```

#### 2. 组件懒加载
```typescript
const HeavyComponent = defineAsyncComponent(() =>
  import('@/components/HeavyComponent.vue')
);
```

#### 3. 图片懒加载
```vue
<template>
  <img
    :src="imageSrc"
    :alt="imageAlt"
    loading="lazy"
    class="w-full h-auto"
  />
</template>
```

#### 4. 预加载关键资源
```html
<link rel="preload" href="/fonts/main.woff2" as="font" type="font/woff2" crossorigin>
<link rel="preload" href="/avatar.jpg" as="image">
```

### 渲染优化

#### 1. 虚拟滚动
```vue
<script setup lang="ts">
import { useVirtualList } from '@vueuse/core';

const { list, containerProps, wrapperProps } = useVirtualList(
  largeList,
  { itemHeight: 100 }
);
</script>
```

#### 2. 防抖和节流
```typescript
import { useDebounceFn, useThrottleFn } from '@vueuse/core';

// 防抖
const handleSearch = useDebounceFn((value: string) => {
  // 搜索逻辑
}, 300);

// 节流
const handleScroll = useThrottleFn(() => {
  // 滚动逻辑
}, 100);
```

#### 3. requestAnimationFrame
```typescript
const animate = () => {
  // 动画逻辑
  requestAnimationFrame(animate);
};
```

### 资源优化

#### 1. 图片优化
```vue
<template>
  <!-- 使用WebP格式 -->
  <picture>
    <source :srcset="webpSrc" type="image/webp" />
    <img :src="jpgSrc" :alt="alt" />
  </picture>

  <!-- 响应式图片 -->
  <img
    :srcset="`${src}?w=400 400w, ${src}?w=800 800w, ${src}?w=1200 1200w`"
    sizes="(max-width: 600px) 400px, (max-width: 1200px) 800px, 1200px"
    :src="src"
    :alt="alt"
  />
</template>
```

#### 2. 代码分割
```typescript
// vite.config.ts
export default defineConfig({
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          'vue-vendor': ['vue', 'vue-router', 'pinia'],
          'utils': ['lodash-es', 'dayjs'],
        },
      },
    },
  },
});
```

#### 3. Tree Shaking
```typescript
// ✅ 推荐:按需引入
import { debounce } from 'lodash-es';

// ❌ 不推荐:引入整个库
import _ from 'lodash-es';
```

### 缓存策略

#### 1. 浏览器缓存
```typescript
// vite.config.ts
export default defineConfig({
  build: {
    rollupOptions: {
      output: {
        chunkFileNames: 'assets/js/[name]-[hash].js',
        entryFileNames: 'assets/js/[name]-[hash].js',
        assetFileNames: 'assets/[ext]/[name]-[hash].[ext]',
      },
    },
  },
});
```

#### 2. 数据缓存
```typescript
export const useProjectStore = defineStore('project', {
  state: () => ({
    cache: new Map<string, any>(),
    cacheTime: 5 * 60 * 1000, // 5分钟
  }),

  actions: {
    async loadProjects() {
      const cacheKey = 'projects';
      const cached = this.cache.get(cacheKey);

      if (cached && Date.now() - cached.time < this.cacheTime) {
        return cached.data;
      }

      const data = await fetchProjects();
      this.cache.set(cacheKey, { data, time: Date.now() });
      return data;
    },
  },
});
```

### 性能监控

#### 1. Web Vitals
```typescript
import { getCLS, getFID, getFCP, getLCP, getTTFB } from 'web-vitals';

getCLS(console.log);
getFID(console.log);
getFCP(console.log);
getLCP(console.log);
getTTFB(console.log);
```

#### 2. 性能测量
```typescript
export function usePerformance() {
  const measure = (name: string, fn: () => void) => {
    performance.mark(`${name}-start`);
    fn();
    performance.mark(`${name}-end`);
    performance.measure(name, `${name}-start`, `${name}-end`);

    const measure = performance.getEntriesByName(name)[0];
    console.log(`${name} 耗时: ${measure.duration.toFixed(2)}ms`);

    performance.clearMarks();
    performance.clearMeasures();
  };

  return { measure };
}
```

---

## 🔒 安全考虑

### 1. XSS防护
```typescript
// 使用DOMPurify清理HTML
import DOMPurify from 'dompurify';

const cleanHtml = DOMPurify.sanitize(dirtyHtml);
```

### 2. HTTPS
- 使用HTTPS协议
- 配置HSTS

### 3. CSP(Content Security Policy)
```html
<meta http-equiv="Content-Security-Policy" content="
  default-src 'self';
  script-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net;
  style-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net;
  img-src 'self' data: https:;
  font-src 'self' https://cdn.jsdelivr.net;
  connect-src 'self' https:;
">
```

### 4. 输入验证
```typescript
function validateEmail(email: string): boolean {
  const re = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
  return re.test(email);
}
```

---

## 🚀 部署架构

### 部署流程

```
开发环境 → Git提交 → GitHub Actions → 构建测试 → Vercel部署 → 生产环境
```

### CI/CD配置

#### GitHub Actions
```yaml
# .github/workflows/deploy.yml
name: Deploy to Vercel

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install

      - name: Run tests
        run: pnpm run test

      - name: Build
        run: pnpm run build

      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v20
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.ORG_ID }}
          vercel-project-id: ${{ secrets.PROJECT_ID }}
          vercel-args: '--prod'
```

### Vercel配置

```json
// vercel.json
{
  "buildCommand": "pnpm run build",
  "outputDirectory": "dist",
  "devCommand": "pnpm run dev",
  "installCommand": "pnpm install",
  "framework": "vite",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        },
        {
          "key": "X-XSS-Protection",
          "value": "1; mode=block"
        }
      ]
    }
  ],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

---

## 📊 监控和分析

### 1. 错误监控(可选)
```typescript
// 集成Sentry
import * as Sentry from '@sentry/vue';

app.use(Sentry.init({
  dsn: import.meta.env.VITE_SENTRY_DSN,
  environment: import.meta.env.MODE,
}));
```

### 2. 用户分析(可选)
```typescript
// 集成Google Analytics
import VueGtag from 'vue-gtag';

app.use(VueGtag, {
  config: {
    id: import.meta.env.VITE_GA_ID,
  },
});
```

---

## 🎯 总结

本技术架构设计方案提供了:

1. **清晰的架构分层**: 从UI到基础设施,层次分明
2. **合理的技术选型**: 基于现代化、高性能的技术栈
3. **完整的数据流设计**: 从数据存储到组件展示
4. **全面的性能优化**: 覆盖加载、渲染、缓存等方面
5. **安全考虑**: 防护常见安全威胁
6. **完善的部署方案**: CI/CD和Vercel部署配置

通过本架构方案,可以构建一个高性能、可维护、可扩展的个人作品集网站。

---

**文档版本**: v1.0
**最后更新**: 2026-04-12
**架构师**: 罗雍来