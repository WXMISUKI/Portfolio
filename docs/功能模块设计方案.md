# 功能模块设计方案

## 📋 文档信息

- **项目名称**: 个人作品集网站
- **文档版本**: v1.0
- **创建日期**: 2026-04-12
- **设计师**: 罗雍来

---

## 🏠 1. 首页模块(Home)

### 模块概述
首页是网站的门面,需要快速抓住访客注意力,展示核心信息和引导用户深入了解。

### 功能组件

#### 1.1 英雄区(HeroSection)

**设计目标**:
- 展示个人品牌形象
- 突出核心技能和定位
- 引导用户浏览更多内容

**布局设计**:
```
┌─────────────────────────────────────────────────────┐
│  [个人头像]        罗雍来                           │
│  (圆形,发光)       前端工程师・AI 全栈开发者        │
│                   擅长政企复杂业务系统、GIS 地图...   │
│                   [政企项目][GIS][视频监控]...       │
│                   [查看项目→] [下载简历]              │
│  [GitHub][LinkedIn][邮箱]  [↓ 向下滚动]             │
└─────────────────────────────────────────────────────┘
```

**组件结构**:
```vue
<template>
  <section class="hero-section">
    <div class="container-custom">
      <div class="hero-content">
        <!-- 左侧:头像 -->
        <div class="hero-avatar">
          <img src="/avatar.jpg" alt="罗雍来" />
          <div class="avatar-glow"></div>
        </div>

        <!-- 右侧:内容 -->
        <div class="hero-text">
          <h1 class="hero-title">罗雍来</h1>
          <h2 class="hero-subtitle">
            <span v-for="(word, index) in typedWords" :key="index">
              {{ word }}
            </span>
            <span class="cursor">|</span>
          </h2>
          <p class="hero-description">
            {{ profile.description }}
          </p>

          <!-- 标签 -->
          <div class="hero-tags">
            <span
              v-for="tag in profile.tags"
              :key="tag"
              class="tag"
            >
              {{ tag }}
            </span>
          </div>

          <!-- 按钮 -->
          <div class="hero-actions">
            <Button variant="primary" @click="goToProjects">
              查看项目 →
            </Button>
            <Button variant="secondary" @click="downloadResume">
              下载简历
            </Button>
          </div>

          <!-- 社交链接 -->
          <div class="hero-social">
            <a
              v-for="social in socialLinks"
              :key="social.name"
              :href="social.url"
              :aria-label="social.name"
              target="_blank"
            >
              <Icon :name="social.icon" />
            </a>
          </div>
        </div>
      </div>

      <!-- 滚动提示 -->
      <div class="scroll-indicator">
        <div class="scroll-arrow"></div>
      </div>
    </div>
  </section>
</template>

<script setup lang="ts">
import { ref, onMounted, onUnmounted } from 'vue';
import { useRouter } from 'vue-router';

const router = useRouter();
const profile = ref(profileData);
const socialLinks = ref(socialData);
const typedWords = ref(['前端工程师']);
let wordIndex = 0;

// 打字机效果
const typeWriter = () => {
  const words = ['前端工程师', 'AI 全栈开发者', '性能优化专家'];
  if (wordIndex < words.length) {
    typedWords.value = [words[wordIndex]];
    wordIndex = (wordIndex + 1) % words.length;
  }
  setTimeout(typeWriter, 3000);
};

const goToProjects = () => {
  router.push('/projects');
};

const downloadResume = () => {
  window.open('/resume.pdf', '_blank');
};

onMounted(() => {
  typeWriter();
});
</script>

<style scoped>
.hero-section {
  min-height: 100vh;
  display: flex;
  align-items: center;
  position: relative;
  overflow: hidden;
}

.hero-avatar {
  width: 300px;
  height: 300px;
  border-radius: 50%;
  overflow: hidden;
  position: relative;
  box-shadow: 0 0 60px rgba(6, 182, 212, 0.3);
}

.avatar-glow {
  position: absolute;
  inset: 0;
  border-radius: 50%;
  background: radial-gradient(circle, rgba(6, 182, 212, 0.2) 0%, transparent 70%);
  animation: pulse 3s ease-in-out infinite;
}

@keyframes pulse {
  0%, 100% {
    transform: scale(1);
    opacity: 0.5;
  }
  50% {
    transform: scale(1.1);
    opacity: 0.8;
  }
}

.hero-content {
  display: grid;
  grid-template-columns: 300px 1fr;
  gap: 60px;
  align-items: center;
}

/* 响应式 */
@media (max-width: 768px) {
  .hero-content {
    grid-template-columns: 1fr;
    text-align: center;
  }

  .hero-avatar {
    margin: 0 auto;
  }
}
</style>
```

#### 1.2 快速链接(QuickLinks)

**功能**:
- 展示核心页面快速入口
- 突出重要功能

**布局设计**:
```
┌─────────────────────────────────────────────────────┐
│  [关于我] [项目经验] [技术栈] [联系我]               │
└─────────────────────────────────────────────────────┘
```

---

## 👤 2. 关于我模块(About)

### 模块概述
展示个人信息、职业经历、技能水平,让访客全面了解。

### 功能组件

#### 2.1 个人资料卡(ProfileCard)

**功能**:
- 展示基本信息
- 显示求职期望
- 展示社交链接

**布局设计**:
```
┌─────────────────────────────────────────────────────┐
│  ┌──────────┐  姓名:罗雍来                         │
│  │  头像    │  职位:前端工程师・AI全栈开发者       │
│  └──────────┘  经验:8个月+政企项目 + 7个月+AI开发    │
│               期望职位:智能体开发/大模型应用/全栈   │
│               期望城市:杭州/舟山                     │
│               期望薪资:12k-16k                      │
│               [GitHub][LinkedIn][邮箱]              │
└─────────────────────────────────────────────────────┘
```

#### 2.2 技能雷达图(SkillRadar)

**功能**:
- 可视化展示技能水平
- 直观显示能力分布

**技术实现**:
```vue
<script setup lang="ts">
import { ref, onMounted } from 'vue';
import * as echarts from 'echarts';

const chartRef = ref<HTMLElement>();

onMounted(() => {
  const chart = echarts.init(chartRef.value!);

  const option = {
    radar: {
      indicator: [
        { name: '前端开发', max: 100 },
        { name: '后端开发', max: 100 },
        { name: 'GIS开发', max: 100 },
        { name: 'AI开发', max: 100 },
        { name: '系统架构', max: 100 },
      ],
      shape: 'circle',
      splitArea: {
        areaStyle: {
          color: ['rgba(6, 182, 212, 0.1)', 'rgba(6, 182, 212, 0.2)'],
        },
      },
      axisLine: {
        lineStyle: {
          color: 'rgba(6, 182, 212, 0.3)',
        },
      },
    },
    series: [
      {
        type: 'radar',
        data: [
          {
            value: [90, 75, 85, 80, 70],
            name: '技能水平',
            areaStyle: {
              color: 'rgba(6, 182, 212, 0.3)',
            },
            lineStyle: {
              color: '#06B6D4',
            },
          },
        ],
      },
    ],
  };

  chart.setOption(option);

  // 响应式
  window.addEventListener('resize', () => {
    chart.resize();
  });
});
</script>
```

#### 2.3 时间线(Timeline)

**功能**:
- 展示项目经历
- 按时间顺序排列

**布局设计**:
```
┌─────────────────────────────────────────────────────┐
│  ● 2025.12-2026.04  新奥海底管道预警系统            │
│    职位:前端核心负责人                              │
│    亮点:三级预警、视频延迟1s、性能提升95%           │
│                                                      │
│  ● 2024.09-2025.12  舟山大桥综管指挥平台            │
│    职位:前端核心开发                                │
│    亮点:日报系统、视频监控、预警管理                │
│                                                      │
│  ● 2025.10-至今  嘉兴公安海防反走私系统              │
│    职位:前端开发工程师                              │
│    亮点:船舶监控、电子围栏、性能优化                │
└─────────────────────────────────────────────────────┘
```

#### 2.4 数据统计(Statistics)

**功能**:
- 展示量化成果
- 使用数字动画

**布局设计**:
```
┌─────────────────────────────────────────────────────┐
│  [8+] 项目数量    [500+] 代码提交    [20+] 技术文档 │
│  [95%] 性能提升   [60fps] 渲染帧率    [1-2s] 视频延迟│
└─────────────────────────────────────────────────────┘
```

**技术实现**:
```vue
<script setup lang="ts">
import { ref, onMounted } from 'vue';
import { useCountUp } from '@/composables/useCountUp';

const stats = ref([
  { label: '项目数量', value: 8, suffix: '+', color: '#06B6D4' },
  { label: '代码提交', value: 500, suffix: '+', color: '#10B981' },
  { label: '技术文档', value: 20, suffix: '+', color: '#F59E0B' },
  { label: '性能提升', value: 95, suffix: '%', color: '#EF4444' },
]);

onMounted(() => {
  stats.value.forEach((stat, index) => {
    useCountUp(stat, 2000);
  });
});
</script>
```

---

## 📁 3. 项目展示模块(Projects)

### 模块概述
展示项目经验,突出技术实力和成果。

### 功能组件

#### 3.1 项目筛选(ProjectFilter)

**功能**:
- 按类别筛选
- 按技术栈筛选
- 搜索功能

**布局设计**:
```
┌─────────────────────────────────────────────────────┐
│  [全部] [政企项目] [AI项目] [个人项目]              │
│  [Vue3] [TypeScript] [GIS] [AI] [+]                │
│  [搜索项目...]                                      │
└─────────────────────────────────────────────────────┘
```

**组件结构**:
```vue
<template>
  <div class="project-filter">
    <!-- 类别筛选 -->
    <div class="filter-category">
      <Button
        v-for="category in categories"
        :key="category.id"
        :variant="currentCategory === category.id ? 'primary' : 'ghost'"
        @click="setCategory(category.id)"
      >
        {{ category.name }}
      </Button>
    </div>

    <!-- 技术栈筛选 -->
    <div class="filter-tech">
      <Button
        v-for="tech in techStacks"
        :key="tech"
        :variant="selectedTechs.includes(tech) ? 'primary' : 'ghost'"
        size="small"
        @click="toggleTech(tech)"
      >
        {{ tech }}
      </Button>
    </div>

    <!-- 搜索 -->
    <div class="filter-search">
      <input
        v-model="searchQuery"
        type="text"
        placeholder="搜索项目..."
        @input="handleSearch"
      />
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, computed } from 'vue';
import { useProjectStore } from '@/store/modules';

const projectStore = useProjectStore();

const categories = ref([
  { id: 'all', name: '全部' },
  { id: 'government', name: '政企项目' },
  { id: 'ai', name: 'AI项目' },
  { id: 'personal', name: '个人项目' },
]);

const currentCategory = ref('all');
const selectedTechs = ref<string[]>([]);
const searchQuery = ref('');

const techStacks = computed(() => projectStore.allTechStacks);

const setCategory = (id: string) => {
  currentCategory.value = id;
  projectStore.setFilter({
    category: id === 'all' ? undefined : id,
  });
};

const toggleTech = (tech: string) => {
  const index = selectedTechs.value.indexOf(tech);
  if (index > -1) {
    selectedTechs.value.splice(index, 1);
  } else {
    selectedTechs.value.push(tech);
  }
  projectStore.setFilter({ techStack: selectedTechs.value });
};

const handleSearch = () => {
  projectStore.setFilter({ search: searchQuery.value });
};
</script>
```

#### 3.2 项目卡片(ProjectCard)

**功能**:
- 展示项目基本信息
- 显示关键亮点
- 提供查看详情入口

**布局设计**:
```
┌─────────────────────────────────────────────────────┐
│  [项目截图 - 轮播]                                  │
│  新奥海底管道预警系统                     [政企项目]  │
│  前端核心负责人                                       │
│  [Vue3][TypeScript][OpenLayers][SRS][FFmpeg]        │
│                                                      │
│  ✅ 独立实现船舶实时监控、三级预警、电子围栏          │
│  ✅ 部署 SRS + FFmpeg,将视频延迟控制在 1s 内         │
│  ✅ 优化 1000+ 船舶渲染,首屏从 3~5 分钟 → 2~3 秒    │
│                                                      │
│  [查看详情] [查看代码]                               │
└─────────────────────────────────────────────────────┘
```

**组件结构**:
```vue
<template>
  <div class="project-card" @click="openDetail">
    <!-- 项目截图 -->
    <div class="project-screenshots">
      <Carousel :images="project.screenshots" />
    </div>

    <!-- 项目信息 -->
    <div class="project-info">
      <div class="project-header">
        <h3 class="project-title">{{ project.title }}</h3>
        <Badge :variant="getCategoryVariant(project.category)">
          {{ getCategoryName(project.category) }}
        </Badge>
      </div>

      <p class="project-role">{{ project.role }}</p>

      <!-- 技术栈 -->
      <div class="project-tech">
        <Tag v-for="tech in project.techStack.slice(0, 5)" :key="tech">
          {{ tech }}
        </Tag>
        <Tag v-if="project.techStack.length > 5">
          +{{ project.techStack.length - 5 }}
        </Tag>
      </div>

      <!-- 亮点 -->
      <div class="project-highlights">
        <div
          v-for="(highlight, index) in project.highlights.slice(0, 3)"
          :key="index"
          class="highlight-item"
        >
          <Icon name="check" />
          <span>{{ highlight }}</span>
        </div>
      </div>

      <!-- 性能指标 -->
      <div v-if="project.metrics" class="project-metrics">
        <div v-if="project.metrics.performance" class="metric">
          <span class="metric-label">性能提升</span>
          <span class="metric-value">{{ project.metrics.performance }}</span>
        </div>
        <div v-if="project.metrics.codeLines" class="metric">
          <span class="metric-label">代码量</span>
          <span class="metric-value">{{ project.metrics.codeLines }}</span>
        </div>
      </div>

      <!-- 操作按钮 -->
      <div class="project-actions">
        <Button variant="primary" @click.stop="openDetail">
          查看详情
        </Button>
        <Button
          v-if="project.repo"
          variant="secondary"
          @click.stop="openRepo"
        >
          查看代码
        </Button>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
import { defineProps } from 'vue';
import { useRouter } from 'vue-router';

const props = defineProps<{
  project: Project;
}>();

const router = useRouter();

const openDetail = () => {
  // 打开详情模态框
};

const openRepo = () => {
  window.open(props.project.repo, '_blank');
};

const getCategoryName = (category: string) => {
  const map = {
    government: '政企项目',
    ai: 'AI项目',
    personal: '个人项目',
  };
  return map[category] || category;
};

const getCategoryVariant = (category: string) => {
  const map = {
    government: 'primary',
    ai: 'success',
    personal: 'warning',
  };
  return map[category] || 'default';
};
</script>
```

#### 3.3 项目详情模态框(ProjectDetailModal)

**功能**:
- 展示完整项目信息
- 显示技术架构
- 展示详细成果

**布局设计**:
```
┌─────────────────────────────────────────────────────┐
│  [×] 新奥海底管道预警系统                             │
│  ┌──────────────────┐  ┌──────────────────┐         │
│  │  项目截图        │  │  项目信息        │         │
│  │  [轮播]          │  │  类型:海事监控... │         │
│  │                  │  │  角色:前端负责人  │         │
│  │                  │  │  周期:5个月      │         │
│  └──────────────────┘  └──────────────────┘         │
│                                                      │
│  📋 项目概述                                         │
│  基于GIS的海底管道预警监控平台...                    │
│                                                      │
│  🛠️ 技术栈                                          │
│  [Vue3][TypeScript][OpenLayers][SRS][FFmpeg]...     │
│                                                      │
│  ✨ 核心亮点                                         │
│  1. 独立实现船舶实时监控、三级预警、电子围栏         │
│  2. 部署 SRS + FFmpeg,将视频延迟控制在 1s 内        │
│  3. 优化 1000+ 船舶渲染,首屏从 3~5 分钟 → 2~3 秒   │
│                                                      │
│  📊 项目成果                                         │
│  - 性能提升:95%                                     │
│  - 代码量:15,000+行                                 │
│  - 文档:20+份                                       │
│                                                      │
│  🔗 相关链接                                         │
│  [GitHub仓库] [在线演示] [技术文档]                  │
└─────────────────────────────────────────────────────┘
```

---

## 🛠️ 4. 技术栈模块(Skills)

### 模块概述
展示技术栈和技能水平,体现技术深度。

### 功能组件

#### 4.1 技术分类(SkillCategory)

**功能**:
- 按类别展示技术栈
- 支持展开/收起

**布局设计**:
```
┌─────────────────────────────────────────────────────┐
│  ▼ 前端核心                                         │
│  [Vue3⭐⭐⭐⭐⭐] [TypeScript⭐⭐⭐⭐⭐]           │
│  [Vite⭐⭐⭐⭐⭐] [Pinia⭐⭐⭐⭐⭐]                 │
│                                                      │
│  ▼ 地图&GIS                                         │
│  [OpenLayers⭐⭐⭐⭐⭐] [MapLibre GL⭐⭐⭐⭐]       │
│  [Cesium⭐⭐⭐] [Turf.js⭐⭐⭐⭐⭐]                 │
│                                                      │
│  ▼ AI&大模型                                        │
│  [LangChain⭐⭐⭐⭐] [LangGraph⭐⭐⭐⭐]            │
│  [Ollama⭐⭐⭐⭐] [DeepSeek⭐⭐⭐⭐]                │
└─────────────────────────────────────────────────────┘
```

#### 4.2 技能进度条(SkillProgress)

**功能**:
- 展示技能熟练度
- 使用动画效果

**技术实现**:
```vue
<template>
  <div class="skill-progress">
    <div class="skill-header">
      <span class="skill-name">{{ skill.name }}</span>
      <span class="skill-level">{{ skill.proficiency }}/5</span>
    </div>
    <div class="skill-bar">
      <div
        class="skill-fill"
        :style="{ width: progress + '%' }"
      ></div>
    </div>
    <div class="skill-stars">
      <Icon
        v-for="i in 5"
        :key="i"
        :name="i <= skill.proficiency ? 'star-filled' : 'star'"
        :class="{ filled: i <= skill.proficiency }"
      />
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue';
import { useCountUp } from '@/composables/useCountUp';

const props = defineProps<{
  skill: Skill;
}>();

const progress = ref(0);

onMounted(() => {
  useCountUp(
    { value: progress },
    (props.skill.proficiency / 5) * 100,
    1500
  );
});
</script>
```

#### 4.3 学习时间线(LearningTimeline)

**功能**:
- 展示技术学习路径
- 按时间顺序排列

**布局设计**:
```
┌─────────────────────────────────────────────────────┐
│  ● 2025.08  开始学习Vue3                            │
│    - 学习Composition API                            │
│    - 实践项目:新奥海底管道预警系统                   │
│                                                      │
│  ● 2025.10  深入TypeScript                         │
│    - 掌握类型系统                                   │
│    - 实践项目:嘉兴公安海防反走私系统                 │
│                                                      │
│  ● 2025.12  探索AI开发                              │
│    - 学习LangChain和LangGraph                       │
│    - 实践项目:MyPrivateAgent私有化助手               │
└─────────────────────────────────────────────────────┘
```

---

## 📞 5. 联系我模块(Contact)

### 模块概述
提供联系方式,促进沟通和合作。

### 功能组件

#### 5.1 联系表单(ContactForm)

**功能**:
- 发送消息
- 表单验证
- 提交反馈

**布局设计**:
```
┌─────────────────────────────────────────────────────┐
│  姓名: [________________]                           │
│  邮箱: [________________]                           │
│  主题: [________________]                           │
│  消息: [________________]                           │
│        [________________]                           │
│        [________________]                           │
│  [发送消息]                                         │
└─────────────────────────────────────────────────────┘
```

**技术实现**:
```vue
<template>
  <form class="contact-form" @submit.prevent="handleSubmit">
    <div class="form-group">
      <label for="name">姓名</label>
      <input
        id="name"
        v-model="form.name"
        type="text"
        required
        :class="{ error: errors.name }"
      />
      <span v-if="errors.name" class="error-message">
        {{ errors.name }}
      </span>
    </div>

    <div class="form-group">
      <label for="email">邮箱</label>
      <input
        id="email"
        v-model="form.email"
        type="email"
        required
        :class="{ error: errors.email }"
      />
      <span v-if="errors.email" class="error-message">
        {{ errors.email }}
      </span>
    </div>

    <div class="form-group">
      <label for="subject">主题</label>
      <input
        id="subject"
        v-model="form.subject"
        type="text"
        required
      />
    </div>

    <div class="form-group">
      <label for="message">消息</label>
      <textarea
        id="message"
        v-model="form.message"
        rows="5"
        required
      ></textarea>
    </div>

    <Button type="submit" :loading="isSubmitting">
      {{ isSubmitting ? '发送中...' : '发送消息' }}
    </Button>
  </form>
</template>

<script setup lang="ts">
import { ref, reactive } from 'vue';

const form = reactive({
  name: '',
  email: '',
  subject: '',
  message: '',
});

const errors = reactive({
  name: '',
  email: '',
});

const isSubmitting = ref(false);

const validateForm = () => {
  errors.name = '';
  errors.email = '';

  if (!form.name) {
    errors.name = '请输入姓名';
    return false;
  }

  if (!form.email || !validateEmail(form.email)) {
    errors.email = '请输入有效的邮箱地址';
    return false;
  }

  return true;
};

const validateEmail = (email: string) => {
  const re = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
  return re.test(email);
};

const handleSubmit = async () => {
  if (!validateForm()) return;

  isSubmitting.value = true;

  try {
    // 使用Formspree或EmailJS发送邮件
    const response = await fetch('https://formspree.io/f/YOUR_FORM_ID', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
      },
      body: JSON.stringify(form),
    });

    if (response.ok) {
      alert('消息发送成功!');
      // 重置表单
      Object.assign(form, {
        name: '',
        email: '',
        subject: '',
        message: '',
      });
    } else {
      alert('发送失败,请稍后重试');
    }
  } catch (error) {
    alert('发送失败,请稍后重试');
  } finally {
    isSubmitting.value = false;
  }
};
</script>
```

#### 5.2 联系卡片(ContactCard)

**功能**:
- 展示联系方式
- 快速复制

**布局设计**:
```
┌─────────────────────────────────────────────────────┐
│  📞 电话                                            │
│  19042637070                          [复制]        │
│                                                      │
│  📧 邮箱                                            │
│  19042637070@139.com                    [复制]      │
│                                                      │
│  🔗 GitHub                                          │
│  https://github.com/WXMISUKI            [访问]      │
│                                                      │
│  💼 LinkedIn                                        │
│  https://linkedin.com/in/username        [访问]     │
└─────────────────────────────────────────────────────┘
```

#### 5.3 社交链接(SocialLinks)

**功能**:
- 展示社交媒体
- 快速跳转

**布局设计**:
```
┌─────────────────────────────────────────────────────┐
│  [GitHub] [LinkedIn] [知乎] [掘金] [微信公众号]     │
└─────────────────────────────────────────────────────┘
```

---

## 🧩 6. 通用组件模块(Shared)

### 6.1 布局组件

#### TheHeader
```vue
<template>
  <header class="header">
    <div class="container-custom">
      <div class="header-content">
        <!-- Logo -->
        <div class="header-logo">
          <router-link to="/">
            <span class="logo-text">罗雍来</span>
          </router-link>
        </div>

        <!-- 导航 -->
        <nav class="header-nav">
          <router-link
            v-for="item in navItems"
            :key="item.path"
            :to="item.path"
            class="nav-link"
            :class="{ active: isActive(item.path) }"
          >
            {{ item.name }}
          </router-link>
        </nav>

        <!-- 移动端菜单按钮 -->
        <button
          class="mobile-menu-btn"
          @click="toggleMobileMenu"
          aria-label="切换菜单"
        >
          <Icon :name="isMobileMenuOpen ? 'close' : 'menu'" />
        </button>
      </div>
    </div>

    <!-- 移动端菜单 -->
    <transition name="slide">
      <div v-if="isMobileMenuOpen" class="mobile-menu">
        <router-link
          v-for="item in navItems"
          :key="item.path"
          :to="item.path"
          class="mobile-nav-link"
          @click="closeMobileMenu"
        >
          {{ item.name }}
        </router-link>
      </div>
    </transition>
  </header>
</template>

<script setup lang="ts">
import { ref, computed } from 'vue';
import { useRoute } from 'vue-router';
import { useUIStore } from '@/store/modules';

const route = useRoute();
const uiStore = useUIStore();

const navItems = ref([
  { path: '/', name: '首页' },
  { path: '/about', name: '关于我' },
  { path: '/projects', name: '项目经验' },
  { path: '/skills', name: '技术栈' },
  { path: '/contact', name: '联系我' },
]);

const isMobileMenuOpen = computed(() => uiStore.isMobileMenuOpen);

const isActive = (path: string) => {
  return route.path === path;
};

const toggleMobileMenu = () => {
  uiStore.toggleMobileMenu();
};

const closeMobileMenu = () => {
  uiStore.closeMobileMenu();
};
</script>
```

#### TheFooter
```vue
<template>
  <footer class="footer">
    <div class="container-custom">
      <div class="footer-content">
        <div class="footer-section">
          <h3>罗雍来</h3>
          <p>前端工程 & AI 全栈开发者</p>
        </div>

        <div class="footer-section">
          <h4>快速链接</h4>
          <router-link to="/about">关于我</router-link>
          <router-link to="/projects">项目经验</router-link>
          <router-link to="/skills">技术栈</router-link>
          <router-link to="/contact">联系我</router-link>
        </div>

        <div class="footer-section">
          <h4>联系方式</h4>
          <p>邮箱: 19042637070@139.com</p>
          <p>GitHub: WXMISUKI</p>
        </div>
      </div>

      <div class="footer-bottom">
        <p>© 2026 罗雍来. All rights reserved.</p>
      </div>
    </div>
  </footer>
</template>
```

#### BackToTop
```vue
<template>
  <transition name="fade">
    <button
      v-show="isVisible"
      class="back-to-top"
      @click="scrollToTop"
      aria-label="返回顶部"
    >
      <Icon name="arrow-up" />
    </button>
  </transition>
</template>

<script setup lang="ts">
import { ref, onMounted, onUnmounted } from 'vue';
import { useSmoothScroll } from '@/composables/useSmoothScroll';

const isVisible = ref(false);
const { scrollTo } = useSmoothScroll();

const handleScroll = () => {
  isVisible.value = window.scrollY > 300;
};

const scrollToTop = () => {
  scrollTo({ top: 0, behavior: 'smooth' });
};

onMounted(() => {
  window.addEventListener('scroll', handleScroll);
});

onUnmounted(() => {
  window.removeEventListener('scroll', handleScroll);
});
</script>
```

### 6.2 业务组件

#### SectionTitle
```vue
<template>
  <div class="section-title">
    <h2>{{ title }}</h2>
    <p v-if="subtitle">{{ subtitle }}</p>
    <div class="title-underline"></div>
  </div>
</template>

<script setup lang="ts">
defineProps<{
  title: string;
  subtitle?: string;
}>();
</script>
```

#### LoadingSpinner
```vue
<template>
  <div class="loading-spinner">
    <div class="spinner"></div>
    <p v-if="text">{{ text }}</p>
  </div>
</template>

<script setup lang="ts">
defineProps<{
  text?: string;
}>();
</script>
```

### 6.3 UI组件

#### Button
```vue
<template>
  <button
    :class="classes"
    :disabled="disabled || loading"
    @click="handleClick"
  >
    <Icon v-if="loading" name="spinner" class="animate-spin" />
    <Icon v-else-if="icon" :name="icon" />
    <span v-if="$slots.default"><slot /></span>
  </button>
</template>

<script setup lang="ts">
import { computed } from 'vue';

const props = defineProps<{
  variant?: 'primary' | 'secondary' | 'ghost';
  size?: 'small' | 'medium' | 'large';
  disabled?: boolean;
  loading?: boolean;
  icon?: string;
}>();

const emit = defineEmits<{
  click: [event: MouseEvent];
}>();

const classes = computed(() => [
  'btn',
  `btn-${props.variant || 'primary'}`,
  `btn-${props.size || 'medium'}`,
  {
    'btn-disabled': props.disabled,
    'btn-loading': props.loading,
  },
]);

const handleClick = (event: MouseEvent) => {
  if (!props.disabled && !props.loading) {
    emit('click', event);
  }
};
</script>
```

#### Tag
```vue
<template>
  <span :class="classes">
    <slot />
  </span>
</template>

<script setup lang="ts">
import { computed } from 'vue';

const props = defineProps<{
  variant?: 'primary' | 'secondary' | 'success' | 'warning' | 'danger';
}>();

const classes = computed(() => [
  'tag',
  `tag-${props.variant || 'primary'}`,
]);
</script>
```

#### Badge
```vue
<template>
  <span :class="classes">
    <slot />
  </span>
</template>

<script setup lang="ts">
import { computed } from 'vue';

const props = defineProps<{
  variant?: 'primary' | 'secondary' | 'success' | 'warning' | 'danger';
}>();

const classes = computed(() => [
  'badge',
  `badge-${props.variant || 'primary'}`,
]);
</script>
```

---

## 🎯 总结

本功能模块设计方案提供了:

1. **完整的功能模块**: 覆盖首页、关于我、项目展示、技术栈、联系我等核心模块
2. **详细的组件设计**: 每个组件都有明确的布局、结构和实现
3. **交互设计**: 包含筛选、搜索、模态框等交互功能
4. **响应式设计**: 考虑了移动端适配
5. **性能优化**: 使用懒加载、虚拟滚动等技术

通过本方案,可以快速构建功能完善、用户体验优秀的个人作品集网站。

---

**文档版本**: v1.0
**最后更新**: 2026-04-12
**设计师**: 罗雍来